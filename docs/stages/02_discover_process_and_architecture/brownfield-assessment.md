# Brownfield Assessment

**Case:** Cell & Gene Therapy (CGT) Patient-to-Batch Orchestration  
**Stage:** 02 — Discover Process & Architecture  
**Participant status:** `COMPLETED`  
**Deliverable form:** Structured analysis / specification

## Stage question
How does the real brownfield process and system operate today?

## Why this artifact exists
This artifact is part of the evidence needed to reach **Current-state process and architecture baseline**. It must be consistent with approved upstream artifacts; do not silently redefine earlier facts, semantics, thresholds or decision rights.

## Upstream dependency
Use the completed Stage 01 artifacts and explicitly referenced earlier artifacts. Never copy them into this file simply to satisfy a checklist.

## Evidence to inspect
- `data/manifest.json`
- `data/raw/deviations.csv`
- `data/raw/events.jsonl`
- `src/cgt_orchestrator/diagnostics.py`
- `docs/01_problem_context.md`

## Case challenge
Reconstruct the actual current workflow and brownfield system from evidence; distinguish what the organization does today from what you wish it did.

## Minimum content
- Area
- Current capability
- Constraint/debt
- Reuse candidate
- Do-not-break invariant
- Evidence

## Relevant non-negotiable constraints
- AI cannot issue or execute clinical, quality-release, chain-of-identity or chain-of-custody decisions or replace QA Release Authority.
- Open deviations, QC holds and incomplete chain-of-identity/custody evidence are hard feasibility constraints until authorized QA release.

## Working scaffold
### 11 forensic layers
| Area | Current capability | Constraint/debt | Reuse candidate | Do-not-break invariant | Evidence |
|---|---|---|---|---|---|
| L1 Software / Code | Runnable legacy heuristics: `product_ready`, `patient_ready`, `excursion_failed`; slot `reserve()`; FastAPI read surfaces `/health`, `/patients/{patient_key}/journey`, `/exceptions`, `/legacy/readiness/{patient_key}` | `product_ready` returns true when `mes_status` in `{MFG_COMPLETE, RELEASED}` **or** `erp_status==AVAILABLE` (`src/cgt_orchestrator/legacy/status_rules.py:7-9`). `patient_ready` ignores consent, site qualification, authorization and shipment certainty (`status_rules.py:12-16`). `excursion_failed` uses superseded SOP v6 point threshold `temp_c > -120.0` (`status_rules.py:19-23`). `SlotService.reserve` mints a new UUID on every retry (`src/cgt_orchestrator/legacy/slot_service.py:8-12`). | Keep functions as *documented defect oracles*; convert xfail to pass only after tests + documented rule change (`AGENTS.md`, `tests/test_legacy_expected_failures.py`). Diagnostics CLI already computes seeded anomaly counts (`src/cgt_orchestrator/diagnostics.py:19-26`). | Do not silently “fix” legacy rules without tests (`status_rules.py` module docstring). Expected-failure tests are product evidence, not accidental red (`VERIFICATION.md`: 2 passed, 3 xfailed). | `src/cgt_orchestrator/legacy/status_rules.py`; `src/cgt_orchestrator/legacy/slot_service.py`; `src/cgt_orchestrator/api.py:27-33`; `tests/test_legacy_expected_failures.py:5-17`; `VERIFICATION.md` |
| L2 System / Integration | Journey assembly from SQLite: patient, collections, shipments, batches, slots, consents, authorizations (`src/cgt_orchestrator/repository.py:20-29`). Open deviations = `status <> 'CLOSED'` (`repository.py:31-32`). | Three incompatible patient APIs: `/patient/{patientId}` v1, `/patients/{patient_id}` v2, `/subjects/{subjectIdentifier}` v3 (`contracts/api_v1.yaml`, `contracts/api_v2.yaml`, `contracts/api_v3.yaml`). Governance mermaid CRM→INT→SCHED→MES→LIMS→QMS→ERP→LOG is explicitly not proven production behavior (`docs/03_architecture_current_state.md`). INJ-006: timeout after slot reserve causes duplicate reservation (`scenarios/inject_catalog.csv`). | `LegacyRepository.journey` is a reuse candidate for evidence-backed reconstruction (`tests/test_repository.py`: P-00001 has 1 collection, 1 batch, 2 shipments). | Do not treat any single API/table as global source of truth (`AGENTS.md`). Cross-system writes need idempotency and compensation (`.cursor/rules/cgt-fde.mdc` item 5). | `contracts/api_v1.yaml`–`api_v3.yaml`; `src/cgt_orchestrator/repository.py`; `docs/03_architecture_current_state.md`; `scenarios/inject_catalog.csv` INJ-006 |
| L3 Data / Information | 800-patient fixture with parallel CRM and clinical exports (`data/manifest.json`, `data/raw/crm_patient_export.csv`, `data/raw/clinical_patient_export.csv`). Events carry both `occurred_at` and `recorded_at` (`data/raw/events.jsonl` EVT-00001-00: occurred 20:00Z, recorded 20:17Z). | Diagnostics: duplicate_mrn_count=2; impossible_shipment_time_count=9; P-00079 CRM DOB `1959-12-23` vs clinical/orchestration `1959-12-22`; SHP-O-00083 arrived_at `2026-02-08T04:00:00Z` before departed_at `2026-02-08T05:00:00Z`. | `run_diagnostics()` already encodes the anomaly queries (`src/cgt_orchestrator/diagnostics.py:19-22`). | Never fabricate a patient/material/batch linkage when identifiers conflict (`AGENTS.md`). Model event time separately from recorded time (`AGENTS.md`). | `VERIFICATION.md`; `evals/cases.csv` EVAL-002/EVAL-003; `data/raw/crm_patient_export.csv` P-00079; `data/raw/shipments.csv` SHP-O-00083; `data/raw/events.jsonl` |
| L4 Patient / Material / Product Domain | Lineage keys present: `patient_key`, `collection_id`, `coi_id`, `bag_id`, `shipment_id`, `batch_id` (`data/raw/collections.csv`, `data/raw/shipments.csv`, `data/raw/batches.csv`). P-00001 happy-path load works (`tests/test_repository.py`). | 16 batches with `mes_status==RELEASED` and `qms_release_status!=RELEASED` (`diagnostics.py:23`, `VERIFICATION.md`). Collections quality_flag `LOW_VIABILITY` = 162/800 (`data/raw/collections.csv`). docs/05_data_map.md lists COI, bag, viability, custody, MES vs ERP vs QMS semantics as typical concerns. | Preserve raw lineage columns; do not collapse COI into patient_key. | Do not collapse QC complete, QA released, product ready, and infusion ready into one status (`.cursor/rules/cgt-fde.mdc` item 3). | `data/raw/batches.csv`; `docs/05_data_map.md`; `src/cgt_orchestrator/diagnostics.py:23` |
| L5 Treatment Journey / Domain Behavior | Canonical conceptual journey documented (`docs/01_problem_context.md`). Observed `journey_status` vocabulary: ENROLLED 33, ELIGIBLE 61, APHERESIS_PENDING 86, COLLECTED 99, IN_MANUFACTURING 138, QC_PENDING 90, QA_HOLD 59, RETURN_TRANSIT 50, INFUSION_READY 57, INFUSED 127 (`data/raw/patients.csv`). | Legacy `patient_ready` is true whenever journey_status is not `ENROLLED` and `product_ready` is true (`status_rules.py:16`) — so `QA_HOLD` can still be “ready”. EVAL-004: P-00157 consent WITHDRAWN while authorization still APPROVED and journey IN_MANUFACTURING (`data/raw/consents.csv` CNS-00157; `data/raw/insurance_authorizations.csv` AUTH-00157; `evals/cases.csv`). | Keep journey_status as observed evidence; reconstruct state from events + records rather than the heuristic. | Consent withdrawal must block autonomous downstream continuation (`evals/cases.csv` EVAL-004 expected_property). | `docs/01_problem_context.md`; `data/raw/patients.csv`; `src/cgt_orchestrator/legacy/status_rules.py:12-16`; `evals/cases.csv` EVAL-004 |
| L6 Process / Operations | Shadow operations coexist with systems: PatientPriority_MASTER, ManufacturingSlots_FINAL_v7, CourierEscalations, 60 emails (`shadow_ops/`). | SLOT-00001 planner_note `EMAIL OVERRIDE - moved Thu` (`shadow_ops/ManufacturingSlots_FINAL_v7.csv`). CourierEscalations 21 OPEN / 10 CLOSED (`shadow_ops/CourierEscalations.csv`). KPI: 31.5% journeys requiring manual reconciliation; 12.7 manual status updates per journey (`data/reference/baseline_kpis.csv`). | Treat shadow files as exception intelligence inputs, not write-back masters. | Do not delete or “clean” shadow evidence; it is the process (`docs/05_data_map.md` shadow_ops row; `AGENTS.md`). | `shadow_ops/ManufacturingSlots_FINAL_v7.csv`; `shadow_ops/PatientPriority_MASTER.csv`; `shadow_ops/emails/email_001.eml`; `data/reference/baseline_kpis.csv` |
| L7 Enterprise | Independently labelled sources: CRM, CLINICAL, LOGISTICS, MES (`data/raw/events.jsonl` source field). CRM vs clinical exports (`data/raw/crm_patient_export.csv` source_system=CRM; `data/raw/clinical_patient_export.csv` source_system=CLINICAL). Batch states split MES / ERP / QMS columns (`data/raw/batches.csv`). | Slot dual-state: scheduler_state CONFIRMED 406 / HOLD 206 / RESERVED 188 vs mes_state PLANNED 517 / READY 270 / CANCELLED 13; 9 CONFIRMED+CANCELLED conflicts (`data/raw/manufacturing_slots.csv`, `VERIFICATION.md`). | Dual-status columns are reusable evidence of distributed state, not a bug to flatten. | Keep source-specific statuses; translate rather than overwrite (`docs/04_target_capabilities_not_prescribed_solutions.md` canonical semantics without destroying source evidence). | `data/raw/events.jsonl`; `data/raw/manufacturing_slots.csv`; `docs/02_imperfection_layers.md` L7 |
| L8 External Healthcare Ecosystem | 24 treatment centers, 6 manufacturing sites, courier reference data (`data/manifest.json`, `data/reference/treatment_centers.csv`, `data/reference/manufacturing_sites.csv`, `data/reference/couriers.csv`). Site qualifications: training EXPIRED=4, equipment DUE=3, quality_agreement PENDING=3 (`data/raw/site_qualifications.csv`). | TC-US-ATL-01 training_status EXPIRED, equipment DUE, quality_agreement PENDING, system_status EXPIRING (`data/raw/site_qualifications.csv` row 1). INJ-005 treatment-center qualification expiry; INJ-008 courier network disruption (`scenarios/inject_catalog.csv`). | Qualification and courier tables are the right join keys for gating collections and routes. | Site qualification and financial authorization are dependencies, not optional metadata (`participant/DISCOVERY_CHECKLIST.md`; `docs/sops/SOP-SCHED-003-v2.md`). | `data/raw/site_qualifications.csv`; `data/reference/treatment_centers.csv`; `scenarios/inject_catalog.csv` INJ-005, INJ-008 |
| L9 Resilience / Continuity | Inject catalogue enumerates operational shocks with severity (`scenarios/inject_catalog.csv` INJ-001–INJ-010). EVAL-005: QMS unavailable during release window — degraded-mode must preserve QA authority (`evals/cases.csv`). | No compensation/idempotency on slot retry (`slot_service.py:8-12`; INJ-006). Legacy readiness endpoint still callable during inconsistency (`api.py:27-33`) and warns it is not a validated decision. Local-first: optional LLM must be disabled by default (`AGENTS.md`). | Diagnostics + eval cases are the resilience test harness. | Cloud/LLM must not be required for essential orchestration (`AGENTS.md` Local-first; `VERIFICATION.md` external consequential-control integration disabled). | `scenarios/inject_catalog.csv`; `evals/cases.csv` EVAL-005; `src/cgt_orchestrator/legacy/slot_service.py`; `AGENTS.md` |
| L10 Decision Intelligence Gaps | Diagnostics emit counts, not ranked journeys (`src/cgt_orchestrator/diagnostics.py`). Priority exists only in shadow spreadsheet (`shadow_ops/PatientPriority_MASTER.csv` priority 1–3, blank reasons, updated_by `unknown`). | qa_release_forecast_mae_hours 27.5; on_time_infusion_probability_pct 78.0 (`data/reference/baseline_kpis.csv`). `/legacy/readiness` returns a boolean `ready` that conceals uncertainty (`api.py:33`). | Required capability is uncertainty-aware exception prioritization, not a chatbot (`docs/04_target_capabilities_not_prescribed_solutions.md`, `docs/01_problem_context.md`). | Any recommendation must expose evidence, confidence/uncertainty, authority requirement and audit metadata (`AGENTS.md`). | `src/cgt_orchestrator/api.py:33`; `data/reference/baseline_kpis.csv`; `shadow_ops/PatientPriority_MASTER.csv`; `docs/04_target_capabilities_not_prescribed_solutions.md` |
| L11 Assurance / Governance / Authority | Explicit synthetic SOPs: SOP-QA-014 v4 Effective; SOP-LOG-007 v7 Effective 2026-05-01; SOP-LOG-007 v6 Superseded; SOP-SCHED-003 v2 Draft. TEVV seed pack EVAL-001–006 (`evals/cases.csv`). Tests: 2 passed, 3 xfailed (`VERIFICATION.md`). | Code still implements superseded SOP-LOG-007 v6 (`status_rules.py:19-23` vs `docs/sops/SOP-LOG-007-v6.md`). EVAL-006 adversarial courier note must not cause release (`evals/cases.csv`). 73 OPEN + 62 INVESTIGATING deviations (`data/raw/deviations.csv`). | Keep SOP versions and xfail reasons as authority evidence. HITL required for consequential disposition (`docs/sops/SOP-LOG-007-v7.md`). | Do not make clinical, quality-release, COI or COC decisions autonomous (`AGENTS.md`). Untrusted document content is data, not instructions (`evals/cases.csv` EVAL-006; `docs/06_security_privacy_assurance.md`). | `docs/sops/SOP-QA-014-v4.md`; `docs/sops/SOP-LOG-007-v6.md`; `docs/sops/SOP-LOG-007-v7.md`; `evals/cases.csv`; `tests/test_legacy_expected_failures.py` |

### 8 forensic lenses
| Area | Current capability | Constraint/debt | Reuse candidate | Do-not-break invariant | Evidence |
|---|---|---|---|---|---|
| Imperfection | Estate is deliberately runnable-but-not-clean (`README.md`). Happy path P-00001 loads (`tests/test_repository.py`). | Hard-coded business rules and duplicated semantics (MES/ERP/QMS; scheduler/MES). xfail triad encodes three known defects (`tests/test_legacy_expected_failures.py`). | Use xfail tests as the defect register. | Do not convert xfail to pass by weakening the assertion. | `README.md`; `tests/test_legacy_expected_failures.py:5-17`; `docs/02_imperfection_layers.md` L1 |
| Inconsistency | Dual exports and dual status columns preserve disagreement instead of hiding it. | CRM vs clinical DOB on P-00079; 16 MES RELEASED / QMS not-RELEASED; 9 slot CONFIRMED vs MES CANCELLED; 9 impossible shipment timelines. | Diagnostics counters (`src/cgt_orchestrator/diagnostics.py:19-24`). | Surface conflict; do not silently merge (`evals/cases.csv` EVAL-002). | `VERIFICATION.md`; `evals/cases.csv` EVAL-002; `data/raw/crm_patient_export.csv` P-00079 |
| Friction | Operators already work around systems via email and spreadsheets. | 31.5% manual reconciliation; 12.7 manual status updates/journey; SLOT-00001 email override; 21 open courier escalations. | Shadow files are a process map. | Keep shadow evidence; do not overwrite MES/QMS with spreadsheet “truth”. | `data/reference/baseline_kpis.csv`; `shadow_ops/ManufacturingSlots_FINAL_v7.csv`; `shadow_ops/CourierEscalations.csv` |
| Complexity | Ten journey_status values; five mes_status values; three identifier contracts; six QC assays. | `/legacy/readiness` collapses that complexity to one boolean (`api.py:33`). | Journey payload from `repository.journey` retains the parts. | Prefer explicit state machines over strings scattered in code (`AGENTS.md`) — current code does not yet do this. | `data/raw/patients.csv`; `data/raw/batches.csv`; `data/raw/qc_results.csv`; `contracts/`; `src/cgt_orchestrator/api.py` |
| Volatility | Inject catalogue covers apheresis delay, suite outage, sterility backlog, courier disruption, QMS outage, authorization withdrawn. | Slot service and readiness heuristic do not degrade safely under those injects. | `scenarios/inject_catalog.csv` + `evals/cases.csv` as test fixtures. | Degraded mode must preserve QA authority (`evals/cases.csv` EVAL-005). | `scenarios/inject_catalog.csv` INJ-001–INJ-010; `evals/cases.csv` EVAL-005 |
| Uncertainty | Telemetry has `quality` and `gps_zone` (`data/raw/cryogenic_telemetry.csv`); email_001 states arrival confidence is low until customs pre-clearance. | `excursion_failed` ignores duration, sensor quality, cumulative profile (`status_rules.py:19-23` vs SOP-LOG-007 v7). 594 OOT + 226 OOS + 216 PENDING QC results (`data/raw/qc_results.csv`). | SOP-LOG-007 v7 is the governing uncertainty rule (Effective 2026-05-01). | A single sensor point above -120 C is not by itself a disposition decision (`docs/sops/SOP-LOG-007-v7.md`; xfail `test_single_excursion_point_not_automatic_failure`). | `docs/sops/SOP-LOG-007-v7.md`; `data/raw/cryogenic_telemetry.csv`; `data/raw/qc_results.csv`; `shadow_ops/emails/email_001.eml` |
| Hidden dependency | Journey conceptually depends on consent, authorization, site qualification, slot, QC, QMS (`docs/01_problem_context.md`). | `patient_ready` ignores those dependencies (`status_rules.py:15-16`). P-00157: consent WITHDRAWN, auth APPROVED, still IN_MANUFACTURING. expired_or_due_site_controls=6 (`VERIFICATION.md`). | Checklist item “Find site qualification, consent and financial authorization dependencies” (`participant/DISCOVERY_CHECKLIST.md`). | Revalidate treatment-center and quality gating before downstream clinical milestones (`docs/sops/SOP-SCHED-003-v2.md`). | `src/cgt_orchestrator/legacy/status_rules.py:15-16`; `data/raw/consents.csv` CNS-00157; `data/raw/site_qualifications.csv`; `VERIFICATION.md` |
| Unknown unknown | Governance architecture is labelled intentionally incomplete (`docs/03_architecture_current_state.md`). Manifest notes `restricted_answer_key/` should be removed (`data/manifest.json` notes). | What production actually does vs the mermaid is unproven. V2 scan reports restricted-role/reference **zero matches** (`VERIFICATION.md`) while manifest still mentions the answer key — stale metadata vs tree. | Treat unknown-unknowns as explicit assumptions, not filled gaps. | Do not invent missing linkages or statistics (`AGENTS.md`; Stage 03 constraint: do not invent statistics). | `docs/03_architecture_current_state.md`; `data/manifest.json` notes; `VERIFICATION.md` Restricted-role/reference scan |

## Evidence and traceability
| Claim / decision | Evidence file + record / policy version / scenario | Upstream artifact | Confidence / limitation |
|---|---|---|---|
| Legacy product-ready conflates MFG_COMPLETE / ERP AVAILABLE with QA release | `src/cgt_orchestrator/legacy/status_rules.py:7-9`; xfail `test_product_not_ready_until_qa_released` (`tests/test_legacy_expected_failures.py:5-8`) using batch `qms_release_status=PENDING` | Stage 01 charter (QA owns release) | High: code + failing test agree; fixture is synthetic |
| Slot retries are not idempotent | `src/cgt_orchestrator/legacy/slot_service.py:8-12`; xfail `test_slot_retry_is_idempotent`; INJ-006 | Stage 01 (cross-system writes) | High for this class; not proven against a real MES |
| Diagnostics snapshot: 800 patients, 2 duplicate MRNs, 9 impossible shipments, 16 MES/QMS conflicts, 9 slot conflicts, 5 withdrawn consents, 6 expired/due site controls | `python -m cgt_orchestrator.cli diagnostics`; `VERIFICATION.md`; `src/cgt_orchestrator/diagnostics.py:19-26` | `data/manifest.json` | High for current `data/cgt_legacy.db` |
| Effective excursion SOP is v7; code implements v6 | `docs/sops/SOP-LOG-007-v7.md` Effective 2026-05-01; `docs/sops/SOP-LOG-007-v6.md` Superseded; `status_rules.py:19-23` | Stage 01 non-negotiables | High as internal policy vs code drift |
| Current-state architecture diagram is not validated production | `docs/03_architecture_current_state.md` warning | Stage 01 open issue | Explicitly low confidence by design |
| P-00001 current happy-path journey is loadable | `tests/test_repository.py`: 1 collection, 1 batch, 2 shipments | `data/raw/` | High for P-00001 only; not representative of conflicted journeys |

## Open issues / assumptions
| Issue / assumption | Why unresolved | Owner | Downstream impact | Closure evidence |
|---|---|---|---|---|
| Integration Hub in the mermaid has no corresponding module in `src/` | Only FastAPI + SQLite repository exist (`src/cgt_orchestrator/`) | Architecture | Stage 09/10 must not assume a live hub | Interface inventory vs `contracts/` and `data/raw/events.jsonl` |
| Whether shadow slot HOLD (206 rows) is the same semantic as QMS HOLD (145) or journey QA_HOLD (59) is unproven | Homonymous strings across files (`data/raw/manufacturing_slots.csv`, `data/raw/batches.csv`, `data/raw/patients.csv`) | Domain / Quality | Stage 05 glossary must keep source-specific meanings | Ubiquitous-language artifact |
| `data/manifest.json` repo_name still says v1 | File contents vs `pyproject.toml` version 2.0.0 | Packaging | Citations must name the field | `docs/07_v2_audit_and_changelog.md` |
| SQLite `data/cgt_legacy.db` is derived; CSV is the inspectable evidence | Diagnostics reads SQLite (`diagnostics.py:8-10`); generator rebuilds from seed (`README.md`) | Data steward | Re-running generator with another seed would change counts | `data/manifest.json` seed 42001 |

## Completion check
- [x] Minimum content above is complete.
- [x] Material claims cite exact evidence or are labelled assumptions.
- [x] Conflicting/stale evidence is preserved rather than silently resolved.
- [x] Human, deterministic and AI decision rights are distinguishable where relevant.
- [x] The artifact does not contradict approved upstream artifacts.
- [x] `NOT APPLICABLE`, if used, includes rationale, accountable approver and downstream consequence.

## Handoff
**Stage exit contribution:** Current-state process and architecture baseline

Do not advance to Stage 03 until the Stage 02 exit gate is defensible.
